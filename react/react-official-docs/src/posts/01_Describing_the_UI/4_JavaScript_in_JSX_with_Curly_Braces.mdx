---
title: "04. 중괄호가 있는 JSX에서 자바스크립트 사용하기"
---

# 중괄호가 있는 JSX에서 자바스크립트 사용하기

JSX에서는 `{}` 중괄호를 사용하여 JavaScript 표현식을 삽입할 수 있다.

## ◾️ 중괄호 사용하기: JSX 내부에서 JavaScript 사용하기

- 속성 값에 JavaScript 변수를 전달할 때는 중괄호 `{}`를 사용한다.
- 예: `src={avatar}` → 변수 `avatar`의 값을 읽어 전달  
  반면, `src="{avatar}"`는 `"avatar"`라는 문자열로 처리된다.

  ```text
  import React from "react";

  const Avatar: React.FC = () => {
    const avatar: string = "https://i.imgur.com/7vQD0fPs.jpg";
    return (
      <img
        className="avatar"
        src={avatar}
        alt="Gregorio Y. Zara"
      />
    );
  };

  export default Avatar;
  ```

- JSX 태그 내부에서도 중괄호로 JavaScript 표현식을 삽입할 수 있다.

  ```text
  import React from "react";

  const TodoList: React.FC = () => {
    const name: string = "Gregorio Y. Zara";
    return <h1>{name}&apos;s To Do List</h1>;
  };

  export default TodoList;
  ```

- 함수 호출도 중괄호 안에서 사용할 수 있다.

  ```text
  import React from "react";

  const today: Date = new Date();

  function formatDate(date: Date): string {
    return new Intl.DateTimeFormat("en-US", { weekday: "long" }).format(date);
  }

  const TodoList: React.FC = () => {
    return <h1>To Do List for {formatDate(today)}</h1>;
  };

  export default TodoList;
  ```

## ◾️ 이중 중괄호 사용하기: 객체를 속성으로 전달

- 객체 리터럴을 전달할 때는 중괄호 안에 또 다른 중괄호를 사용한다.
- 대표적인 예는 style 속성이다.
- 스타일 속성명은 camelCase로 작성해야 한다. (예: background-color → backgroundColor)

```text
import React from "react";

const TodoList: React.FC = () => {
  return (
    <ul
      style={{
        backgroundColor: "black",
        color: "pink",
      }}
    >
      <li>Improve the videophone</li>
      <li>Prepare aeronautics lectures</li>
      <li>Work on the alcohol-fuelled engine</li>
    </ul>
  );
};

export default TodoList;
```

- 객체를 미리 선언하고 전달하는 방식도 가능하다.

  ```text
  import React from "react";

  interface Person {
    name: string;
    theme: {
      backgroundColor: string;
      color: string;
    };
  }

  const person: Person = {
    name: "Gregorio Y. Zara",
    theme: {
      backgroundColor: "black",
      color: "pink",
    },
  };

  const TodoList: React.FC = () => {
    return (
      <div style={person.theme}>
        <h1>{person.name}&apos;s Todos</h1>
        <img
          className="avatar"
          src="https://i.imgur.com/7vQD0fPs.jpg"
          alt="Gregorio Y. Zara"
        />
        <ul>
          <li>Improve the videophone</li>
          <li>Prepare aeronautics lectures</li>
          <li>Work on the alcohol-fuelled engine</li>
        </ul>
      </div>
    );
  };

  export default TodoList;
  ```

## ◾️ 참고

- 참고: [React 공식 문서](https://ko.react.dev/learn/javascript-in-jsx-with-curly-braces)
- 챌린지: [챌린지](https://ko.react.dev/learn/javascript-in-jsx-with-curly-braces#challenges)
